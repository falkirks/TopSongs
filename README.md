TopSongs
========

Display and listen to iTunes top charts. Uses iTunes and YouTube API along with JQuery, spin.js, video.js and the youtube extension for video.js. The API interaction is done in PHP and the playback is done in JQuery.
